# apps/api/.env.test.example
# Copiar a .env.test para tests e2e

# Postgres (TEST) — DB separada para evitar borrar DEV
DATABASE_URL_TEST="postgresql://app:app@localhost:5432/app_test?schema=public"

# Shadow DB (TEST) — solo si tu prisma.config realmente la requiere
SHADOW_DATABASE_URL_TEST="postgresql://app:app@localhost:5432/app_shadow_test?schema=public"

NODE_ENV=test
TZ=UTC
